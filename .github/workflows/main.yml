name: Deploy to Sakura Rental Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Decode and set up SSH key
      env:
        SAKURA_PRIVATE_KEY: ${{ secrets.SAKURA_PRIVATE_KEY }}
        SAKURA_HOST: ${{ secrets.SAKURA_HOST }}
        SAKURA_USER: ${{ secrets.SAKURA_USER }}
      run: |
        echo "$SAKURA_PRIVATE_KEY" | base64 --decode > private_key && chmod 600 private_key
        cat private_key
        file private_key
        ssh -v -o StrictHostKeyChecking=no -i private_key ${SAKURA_USER}@${SAKURA_HOST} '
          cd /home/castero/laravel11-test &&
          git pull origin main &&
          composer install --no-dev &&
          php artisan migrate --force
        '
